int main() {
    call_ssa();
    return ssa(5);
}

int ssa(int n) {
    int i = n;
    int r = 1;
    while (i > 1) {
        i = i - 1;
        r = r * i;
    }
    return r;
}

void call_ssa() {
    ssa(1);
    int b = 2;
    if (b >= 2) {
        b = ssa(b);
        return;
    }
}
